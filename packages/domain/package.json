{
    "name": "home-accounts-app-domain-model",
    "version": "1.0.0",
    "type": "module",
    "private": true,
    "types": "library",
    "scripts": {
        "dev": "tsc -w",
        "build": "tsc -p .",
        "test": "jest",
        "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
    },
    "jest": {
        "preset": "jest-config"
    },
    "devDependencies": {
        "@types/jest": "^29.5.1",
        "@types/node": "^18.16.3",
        "jest": "^29.5.0",
        "jest-config": "*",
        "ts-config": "*",
        "typescript": "4.5.4"
    }
}

{
    "$schema": "https://turbo.build/schema.json",
    "extends": ["//"],
    "pipeline": {
        "build": {
            "outputMode": "errors-only"
        },
        "test": {
            "outputMode": "errors-only"
        }
    }
}

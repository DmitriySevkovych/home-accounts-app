/*
    @group integration
 */
describe('Database setup tests', () => {
    it('should do nothing', () => {
        // Arrange
        const x = 2 + 2
        // Act
        // Assert
        expect(x).toBe(4)
    })
})
